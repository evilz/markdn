// Integration tests for running the IIncrementalGenerator via Roslyn adapters are environment
// dependent and were moved to emitter-level tests. This file is intentionally left empty to
// avoid running fragile integration logic in CI environments that may not provide the
// necessary adapter types at runtime.

